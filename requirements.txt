# Minimal dependencies for hydro_analysis (adjust versions as needed)
numpy>=1.24,<2
pandas>=2.0,<3
scipy>=1.10,<2
matplotlib>=3.6,<4
trackpy>=0.6.0,<0.7
pims>=0.6,<0.8
imageio>=2.25,<3
tqdm>=4.65,<5

# Dev / testing (optional)
pytest>=7.2,<8