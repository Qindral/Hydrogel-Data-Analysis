[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "hydro-analysis"
version = "0.1.0"
description = "Hydrogel data analysis tools"
authors = [{name = "Jonas"}]

[tool.setuptools.packages.find]
include = ["hydro_analysis", "hydro_analysis.*"]
